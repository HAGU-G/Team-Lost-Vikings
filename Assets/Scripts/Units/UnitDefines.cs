namespace UNIT
{
    public enum CLASS
    {
        NONE,
        TEST_CLASS
    }

    public enum GROUP
    {
        PLAYER,
        MONSTER
    }

    public enum STATE_ON_DUNGEON
    {
        IDLE,
        TRACE,
        ATTACK,
        GOTO_TOWN,
        DEAD
    }
}